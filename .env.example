# Anthropic — used by LLMService when use_mock=False (THE-5 / THE-11 / THE-12)
ANTHROPIC_API_KEY=

# News search APIs — used by searchers once THE-8 is implemented
NEWSAPI_KEY=
GNEWS_KEY=

# Frontend: override if backend runs on a different port/host
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
